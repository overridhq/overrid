# Integration Scenario Contract

`tests/integration/scenarios` is reserved for source-controlled scenario manifests and notes.

Scenario manifests must cite shared schema versions, fixture refs, expected phase gates, safe timeouts, and assertion rules. They must not embed raw secrets, private payloads, or production endpoints.
