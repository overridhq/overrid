[package]
name = "overrid-local-stack"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Deterministic Rust local development stack helpers for Overrid."

[dependencies]
overrid-contracts = { path = "../schemas/overrid_contracts" }

[lib]
path = "src/lib.rs"

[lints]
workspace = true
