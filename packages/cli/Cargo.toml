[package]
name = "overrid-cli"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Initial Rust CLI skeleton for Overrid."

[dependencies]
overrid-contracts = { path = "../schemas/overrid_contracts" }
overrid-integration-harness = { path = "../integration_harness" }
overrid-sdk = { path = "../sdk" }

[[bin]]
name = "overrid"
path = "src/main.rs"

[lib]
path = "src/lib.rs"

[lints]
workspace = true
